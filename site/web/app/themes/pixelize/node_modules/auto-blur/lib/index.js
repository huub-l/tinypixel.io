'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});
exports.blur = blur;
exports.autoBlur = autoBlur;
function blur(evt) {
    var tag = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'BUTTON';

    if (evt && evt.path) {
        var ancestorButton = evt.path.find(function (el) {
            return el.tagName && el.tagName.toLowerCase() == tag.toLowerCase();
        });
        if (ancestorButton) {
            ancestorButton.blur();
        }
    }
}
function autoBlur() {
    var tag = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 'BUTTON';

    window.addEventListener('click', function (evt) {
        if (evt.path && evt.path.find && evt.path.find(function (el) {
            return el.tagName && el.tagName.toLowerCase() == tag.toLowerCase();
        })) {
            blur(evt, tag);
        }
    });
}